<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	
    <groupId>com.ruoyiliteflow</groupId>
    <artifactId>ruoyi-vue-liteflow</artifactId>
    <version>3.9.2</version>

    <name>ruoyi-vue-liteflow</name>
    <url>http://www.ruoyi.vip</url>
    <description>RuoYi-Vue-LiteFlow 业务编排中台</description>
    
    <properties>
        <ruoyi-vue-liteflow.version>3.9.2</ruoyi-vue-liteflow.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>17</java.version>
        <spring-boot.version>4.0.6</spring-boot.version>
        <mybatis-spring-boot.version>4.0.1</mybatis-spring-boot.version>
        <druid.version>1.2.28</druid.version>
        <yauaa.version>8.1.1</yauaa.version>
        <kaptcha.version>2.3.3</kaptcha.version>
        <pagehelper.boot.version>4.1.0</pagehelper.boot.version>
        <fastjson.version>2.0.62</fastjson.version>
        <oshi.version>7.3.0</oshi.version>
        <commons.io.version>2.22.0</commons.io.version>
        <poi.version>4.1.2</poi.version>
        <velocity.version>2.3</velocity.version>
        <jwt.version>0.9.1</jwt.version>
        <jaxb-api.version>2.3.1</jaxb-api.version>
        <springdoc.version>3.0.3</springdoc.version>
        <liteflow.version>2.16.0</liteflow.version>
        <langchain4j.version>1.17.2</langchain4j.version>
        <langgraph4j.version>1.8.20</langgraph4j.version>
    </properties>

    <!-- 依赖声明 -->
    <dependencyManagement>
        <dependencies>

            <!-- SpringBoot的依赖配置 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- 阿里数据库连接池 -->
            <dependency>
                <groupId>com.alibaba</groupId>
                <artifactId>druid-spring-boot-4-starter</artifactId>
                <version>${druid.version}</version>
            </dependency>

            <!-- 解析客户端操作系统、浏览器等 -->
            <dependency>
                <groupId>nl.basjes.parse.useragent</groupId>
                <artifactId>yauaa</artifactId>
                <version>${yauaa.version}</version>
            </dependency>

            <!-- pagehelper 分页插件 -->
            <dependency>
                <groupId>com.github.pagehelper</groupId>
                <artifactId>pagehelper-spring-boot-starter</artifactId>
                <version>${pagehelper.boot.version}</version>
            </dependency>

            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter</artifactId>
                <version>${mybatis-spring-boot.version}</version>
            </dependency>

            <dependency>
                <groupId>javax.xml.bind</groupId>
                <artifactId>jaxb-api</artifactId>
                <version>${jaxb-api.version}</version>
            </dependency>

            <!-- 获取系统信息 -->
            <dependency>
                <groupId>com.github.oshi</groupId>
                <artifactId>oshi-core</artifactId>
                <version>${oshi.version}</version>
            </dependency>

            <!-- spring-doc -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- io常用工具类 -->
            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons.io.version}</version>
            </dependency>

            <!-- excel工具 -->
            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <!-- velocity代码生成使用模板 -->
            <dependency>
                <groupId>org.apache.velocity</groupId>
                <artifactId>velocity-engine-core</artifactId>
                <version>${velocity.version}</version>
            </dependency>

            <!-- 阿里JSON解析器 -->
            <dependency>
                <groupId>com.alibaba.fastjson2</groupId>
                <artifactId>fastjson2</artifactId>
                <version>${fastjson.version}</version>
            </dependency>

            <!-- Token生成与解析-->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt</artifactId>
                <version>${jwt.version}</version>
            </dependency>

            <!-- 验证码 -->
            <dependency>
                <groupId>pro.fessional</groupId>
                <artifactId>kaptcha</artifactId>
                <version>${kaptcha.version}</version>
            </dependency>

            <!-- 定时任务-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-quartz</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- 代码生成-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-generator</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- 核心模块-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-framework</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- 系统模块-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-system</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- 通用工具-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-common</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- LiteFlow 模块-->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-liteflow</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- LiteFlow Re-Act Agent 扩展 -->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-agent</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- LangChain4j / LangGraph4j 扩展 -->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-langchain</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- AI 内核（无 LiteFlow） -->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-ai-core</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- MCP Server -->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-mcp-server</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <!-- 独立 Chat Agent -->
            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-agent-chat</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-agent-risk</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-agent-rag</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.ruoyiliteflow</groupId>
                <artifactId>ruoyi-vue-liteflow-agent-ops</artifactId>
                <version>${ruoyi-vue-liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>dev.langchain4j</groupId>
                <artifactId>langchain4j-bom</artifactId>
                <version>${langchain4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.bsc.langgraph4j</groupId>
                <artifactId>langgraph4j-bom</artifactId>
                <version>${langgraph4j.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- LiteFlow 规则引擎 -->
            <dependency>
                <groupId>com.yomahub</groupId>
                <artifactId>liteflow-spring-boot4-starter</artifactId>
                <version>${liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.yomahub</groupId>
                <artifactId>liteflow-react-agent-openai</artifactId>
                <version>${liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.yomahub</groupId>
                <artifactId>liteflow-rule-sql</artifactId>
                <version>${liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.yomahub</groupId>
                <artifactId>liteflow-script-groovy</artifactId>
                <version>${liteflow.version}</version>
            </dependency>

            <dependency>
                <groupId>com.yomahub</groupId>
                <artifactId>liteflow-script-qlexpress</artifactId>
                <version>${liteflow.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <modules>
        <module>ruoyi-vue-liteflow-admin</module>
        <module>ruoyi-vue-liteflow-framework</module>
        <module>ruoyi-vue-liteflow-system</module>
        <module>ruoyi-vue-liteflow-quartz</module>
        <module>ruoyi-vue-liteflow-generator</module>
        <module>ruoyi-vue-liteflow-common</module>
        <module>ruoyi-vue-liteflow-liteflow</module>
        <module>ruoyi-vue-liteflow-agent</module>
        <module>ruoyi-vue-liteflow-langchain</module>
        <module>ruoyi-vue-liteflow-ai-core</module>
        <module>ruoyi-vue-liteflow-mcp-server</module>
        <module>ruoyi-vue-liteflow-agent-chat</module>
        <module>ruoyi-vue-liteflow-agent-risk</module>
        <module>ruoyi-vue-liteflow-agent-rag</module>
        <module>ruoyi-vue-liteflow-agent-ops</module>
    </modules>
    <packaging>pom</packaging>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <parameters>true</parameters>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
            </plugin>
        </plugins>
    </build>

    <repositories>
        <repository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
        </repository>
    </repositories>

    <pluginRepositories>
        <pluginRepository>
            <id>public</id>
            <name>aliyun nexus</name>
            <url>https://maven.aliyun.com/repository/public</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </pluginRepository>
    </pluginRepositories>

</project>